export default {
  SET_VISIBLE(state, item) {
    state.visible = item;
  },

  SET_KEY_MENU(state, key) {
    state.keyMenu = key;
  },

  SET_META(state, meta) {
    state.meta = meta;
  },

  SET_IS_MOBILE(state, value) {
    state.isMobile = value;
  },
};
